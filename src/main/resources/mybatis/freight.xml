<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="code.dao.freightDao">
    <insert id="addFreight" parameterType="code.pojo.freight">
        INSERT INTO `shopping`.`freight` (id, type, price, cities) VALUE (#{id},#{type},#{price},#{cities});
    </insert>
    <select id="selectedFreight" resultType="code.pojo.freight">
        select *
        from shopping.freight f
        where f.cities like concat('%',#{cities},'%')
    </select>
    <delete id="deleteFreight" parameterType="String">
        delete from shopping.freight f
        where f.id = #{id}
    </delete>
    <update id="upDataFreight" parameterType="String">
        update shopping.freight f
        set f.price = #{price},f.cities = #{cities},f.type = #{type}
        where f.id = #{id}
    </update>
</mapper>

